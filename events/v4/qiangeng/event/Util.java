package qiangeng.event;

import sun.misc.Unsafe;

public class Util {
	// Just correct your unsafe reference.
	// There is no need to repeat the code to every where
	final public static Unsafe unsafe = Unsafes.unsafe;
}
